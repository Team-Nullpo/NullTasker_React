# NullTasker React - ディレクトリ構造

## 概要

Feature-based と Type-based を組み合わせたハイブリッドなディレクトリ構成に移行しました。

## ディレクトリ構造

```
src/
├── App.tsx                      # ルートコンポーネント
├── main.tsx                     # エントリーポイント
├── vite-env.d.ts               # Vite型定義
│
├── features/                    # 📦 機能ベースのモジュール
│   ├── auth/                    # 認証機能
│   │   ├── components/          # 認証関連コンポーネント
│   │   │   └── AuthDebug.tsx
│   │   ├── contexts/            # 認証コンテキスト
│   │   │   └── AuthContext.tsx
│   │   ├── pages/               # 認証ページ
│   │   │   ├── LoginPage.tsx
│   │   │   └── RegisterPage.tsx
│   │   ├── services/            # 認証API
│   │   │   └── authService.ts
│   │   └── index.ts             # Public API
│   │
│   ├── tickets/                 # チケット/タスク機能
│   │   ├── components/          # チケットコンポーネント
│   │   │   ├── TicketCard.tsx
│   │   │   ├── TicketForm.tsx
│   │   │   └── TicketList.tsx
│   │   ├── services/            # チケットAPI
│   │   │   └── ticketService.ts
│   │   ├── pages/               # チケットページ
│   │   │   └── TicketPage.tsx
│   │   └── index.ts
│   │
│   ├── projects/                # プロジェクト機能
│   │   ├── pages/
│   │   │   └── DashboardPage.tsx
│   │   └── index.ts
│   │
│   ├── calendar/                # カレンダー機能
│   │   ├── pages/
│   │   │   └── CalendarPage.tsx
│   │   └── index.ts
│   │
│   ├── gantt/                   # ガントチャート機能
│   │   ├── pages/
│   │   │   └── GanttPage.tsx
│   │   └── index.ts
│   │
│   ├── users/                   # ユーザー機能
│   │   ├── pages/
│   │   │   └── UserProfilePage.tsx
│   │   └── index.ts
│   │
│   ├── admin/                   # 管理者機能
│   │   ├── pages/
│   │   │   └── AdminPage.tsx
│   │   └── index.ts
│   │
│   └── settings/                # 設定機能
│       ├── pages/
│       │   └── SettingsPage.tsx
│       └── index.ts
│
├── shared/                      # 🔧 共通リソース
│   ├── components/              # 共通コンポーネント
│   │   ├── Layout/
│   │   │   ├── Layout.tsx
│   │   │   └── Layout.module.css
│   │   ├── SideBar/
│   │   │   ├── SideBar.tsx
│   │   │   └── SideBar.module.css
│   │   ├── ProtectedRoute.tsx
│   │   └── index.ts
│   │
│   ├── constants/               # 定数
│   │   ├── routes.ts
│   │   ├── config.ts
│   │   └── index.ts
│   │
│   ├── styles/                  # グローバルスタイル
│   │   ├── index.css
│   │   └── login.css
│   │
│   ├── types/                   # 共通型定義
│   │   └── index.ts
│   │
│   └── utils/                   # ユーティリティ関数
│       ├── date.ts
│       ├── storage.ts
│       ├── validation.ts
│       ├── format.ts
│       └── index.ts
│
└── lib/                         # 🛠️ ライブラリラッパー
    ├── apiClient.ts             # Axios設定
    └── index.ts
```

## 設計原則

### 1. Feature-based (機能ベース)

各機能は `features/` ディレクトリ配下に独立したモジュールとして配置されます。

**利点:**

- 機能ごとに自己完結
- 機能の追加・削除が容易
- チーム開発での責任分担が明確

### 2. Type-based (種類ベース)

共通リソースは `shared/` ディレクトリ配下に種類ごとに配置されます。

**利点:**

- 再利用可能なコードの集約
- 依存関係の明確化
- グローバルな設定の一元管理

### 3. コロケーション (Colocation)

関連するコードを近くに配置します。

**例:**

- コンポーネントとCSSファイルは同じディレクトリ
- 機能固有のサービスは機能ディレクトリ内

## Import パス

パスエイリアス `@` を使用して、クリーンなimportを実現:

```typescript
// 機能からのimport
import { useAuth } from "@/features/auth";
import { TicketCard } from "@/features/tickets";

// 共通リソースからのimport
import { Layout, ProtectedRoute } from "@/shared/components";
import { formatDate } from "@/shared/utils";
import { ROUTES } from "@/shared/constants";
import { Ticket, User } from "@/shared/types";

// ライブラリからのimport
import { apiClient } from "@/lib";
```

## Public API Pattern

各featureは `index.ts` でPublic APIを定義し、内部実装を隠蔽:

```typescript
// features/auth/index.ts
export { AuthProvider, useAuth } from "./contexts/AuthContext";
export { default as LoginPage } from "./pages/LoginPage";
export { default as RegisterPage } from "./pages/RegisterPage";
export * from "./services/authService";
```

## 新機能の追加方法

1. `features/` 配下に新しいディレクトリを作成
2. 必要に応じてサブディレクトリを作成 (components, services, pages, hooks など)
3. `index.ts` でPublic APIを定義
4. `App.tsx` でルーティング設定

## マイグレーション完了

- ✅ 認証機能 (auth)
- ✅ チケット機能 (tickets)
- ✅ プロジェクト機能 (projects)
- ✅ カレンダー機能 (calendar)
- ✅ ガントチャート機能 (gantt)
- ✅ ユーザー機能 (users)
- ✅ 管理者機能 (admin)
- ✅ 設定機能 (settings)
- ✅ 共通コンポーネント (Layout, SideBar, ProtectedRoute)
- ✅ 共通リソース (utils, constants, types)
- ✅ ライブラリ (apiClient)

## 今後の拡張

新機能を追加する際は、以下の構造を推奨:

```
features/new-feature/
├── components/       # 機能固有のコンポーネント
├── hooks/           # 機能固有のカスタムフック
├── services/        # 機能固有のAPIサービス
├── pages/           # 機能のページ
├── types/           # 機能固有の型定義
├── utils/           # 機能固有のユーティリティ
└── index.ts         # Public API
```
